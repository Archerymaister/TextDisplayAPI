rootProject.name = "text-display-api"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        maven("https://repo.projectshard.dev/repository/maven-public/")
    }
}